# Catatan (Bukan File Auto-Loaded MiMoCode)

File ini BUKAN konvensi resmi MiMoCode — konfigurasi resminya ada di `.mimocode/mimocode.json` (dibuat otomatis lewat wizard saat `mimocode` pertama kali dijalankan).

File yang benar-benar auto-loaded MiMoCode setiap sesi: `MEMORY.md` (lihat bagian atas file itu untuk instruksi agent).

File ini dipertahankan sebagai catatan workflow untuk referensi manusia:
Workflow: README.md -> MEMORY.md -> DECISIONS.md -> docs/ -> sprint -> implement -> review -> commit
